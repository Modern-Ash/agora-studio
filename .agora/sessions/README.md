# Sessions

Each directory contains a durable runtime selection and compiled context created by `agora start`.
Conversation history remains external unless its material outcome is recorded in Agora files.
